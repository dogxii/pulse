# Markdown 语法参考

Pulse 支持丰富的 Markdown 语法，让你轻松创作美观的内容。

## 基础语法

### 标题

```markdown
# 一级标题
## 二级标题
### 三级标题
```

### 文本样式

```markdown
**粗体文字** 或 __粗体文字__

*斜体文字* 或 _斜体文字_

~~删除线文字~~

`行内代码`
```

**效果：**
- **粗体文字**
- *斜体文字*
- ~~删除线文字~~
- `行内代码`

### 代码块

使用三个反引号创建代码块，可以指定语言：

````markdown
```javascript
function hello() {
  console.log('Hello, World!');
}
```
````

### 引用块

```markdown
> 这是一个引用块
> 可以包含多行内容
> 适合引用他人的话或重要提示
```

**效果：**
> 这是一个引用块
> 可以包含多行内容

### 分割线

```markdown
---
```

---

## 列表

### 无序列表

```markdown
- 列表项 1
- 列表项 2
- 列表项 3

或使用星号：
* 列表项 A
* 列表项 B
* 列表项 C
```

### 有序列表

```markdown
1. 第一项
2. 第二项
3. 第三项
```

### 任务列表

```markdown
- [x] 已完成的任务
- [x] 另一个已完成的任务
- [ ] 待完成的任务
- [ ] 另一个待完成的任务
```

**效果：**
- [x] 已完成的任务
- [ ] 待完成的任务

## 链接和图片

### 链接

```markdown
[链接文字](https://example.com)

或直接粘贴 URL（自动识别）：
https://example.com
```

### 图片

```markdown
![图片描述](图片URL)
```

**注意：** Pulse 支持直接上传图片，无需手动插入 Markdown 图片语法。

## 表格

```markdown
| 列标题 1 | 列标题 2 | 列标题 3 |
|---------|:--------:|---------:|
| 左对齐   | 居中对齐  | 右对齐   |
| 内容 1   | 内容 2   | 内容 3   |
| 内容 4   | 内容 5   | 内容 6   |
```

**对齐方式：**
- 默认左对齐：`|---------|`
- 居中对齐：`|:-------:|`
- 右对齐：`|---------:|`

**效果：**

| 功能 | 状态 | 优先级 |
|------|:----:|-------:|
| Markdown | ✅ 完成 | 高 |
| 图片上传 | ✅ 完成 | 高 |
| 评论系统 | ✅ 完成 | 中 |

## 组合使用

你可以在列表、引用块、表格等元素中组合使用其他格式：

```markdown
- **粗体列表项** 包含 `代码`
- 列表项中的 [链接](https://example.com)

> 引用块中的 **粗体** 和 *斜体*
```

## 实用技巧

### 换行

- **段落换行**：空一行创建新段落
- **强制换行**：行尾添加两个空格后回车（不推荐）

### Emoji

直接输入 Emoji 表情符号：
😀 🎉 ✨ 🚀 💡 ⚡ 🔥 ❤️

### 特殊字符转义

如需显示 Markdown 特殊字符，使用反斜杠转义：

```markdown
\*不是斜体\*
\[不是链接\]
```

## 注意事项

1. **空行很重要**：段落之间、列表之间、引用块之间需要空行
2. **代码块语言**：指定语言可以获得更好的代码高亮
3. **表格格式**：确保每行的列数一致
4. **任务列表**：`[ ]` 和 `[x]` 中间要有空格

## 快速参考

| 元素 | 语法 |
|------|------|
| 标题 | `# H1` `## H2` `### H3` |
| 粗体 | `**粗体**` 或 `__粗体__` |
| 斜体 | `*斜体*` 或 `_斜体_` |
| 删除线 | `~~删除线~~` |
| 代码 | `` `代码` `` |
| 链接 | `[文字](URL)` |
| 图片 | `![描述](URL)` |
| 引用 | `> 引用` |
| 列表 | `- 项目` 或 `1. 项目` |
| 任务 | `- [ ] 任务` |
| 分割线 | `---` |

## 示例：完整帖子

```markdown
# 项目更新 🚀

今天完成了几个重要功能！

## 新功能

- [x] Markdown 渲染增强
- [x] 任务列表支持
- [x] 表格支持
- [ ] 通知系统

## 技术细节

使用了以下技术栈：

| 技术 | 用途 |
|------|------|
| Vue 3 | 前端框架 |
| TypeScript | 类型安全 |
| Tailwind CSS | 样式 |

代码示例：

\`\`\`typescript
function renderMarkdown(content: string): string {
  return processContent(content);
}
\`\`\`

> **提示：** 所有功能都经过充分测试

---

感谢大家的支持！❤️
```

## 更多资源

- [Markdown 官方指南](https://www.markdownguide.org/)
- [GitHub Flavored Markdown](https://github.github.com/gfm/)
- [CommonMark Spec](https://commonmark.org/)

开始在 Pulse 上创作吧！✨
